import { ObservableInput } from 'rxjs';
/** Delays the server-side render until the input resolves. */
export declare const wait: <T, O extends Promise<T>>(input: O) => O | undefined;
/** Delays the server-side render until the input resolves. */
export declare const wait$: <T, O extends ObservableInput<T>>(input: O) => import("rxjs").Observable<import("rxjs").ObservedValueOf<O>>;
