import { ComponentRef, Type, ViewContainerRef } from '@angular/core';
export interface LayoutComponent<U> {
    page: ViewContainerRef;
}
export declare const mountPage: <T, U>({ compRef, page, layout, }: {
    compRef: ComponentRef<T | LayoutComponent<U>>;
    page: Type<T>;
    layout?: Type<LayoutComponent<U>> | undefined;
}) => {
    pageRef: ComponentRef<T>;
    layoutRef: ComponentRef<LayoutComponent<U>> | null;
};
