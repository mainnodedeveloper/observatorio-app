import { Decorator, ModuleItem, TsType } from '@swc/core';
import { Visitor } from '@swc/core/Visitor.js';
export interface AngularComponentOptions {
    sourceUrl: string;
}
export declare class AngularComponents extends Visitor {
    private options;
    importFiles: {
        url: string;
        identifier: string;
    }[];
    constructor(options: AngularComponentOptions);
    visitTsTypes(nodes: TsType[]): TsType[];
    visitTsType(nodes: TsType): TsType;
    visitModuleItems(items: ModuleItem[]): ModuleItem[];
    visitDecorator(decorator: Decorator): Decorator;
    private transformTemplateUrl;
    private transformStyleUrls;
}
